export interface Event {
  heading: string;
  subHeading?: string;
  link?: string;
  date: string;
  provider: string;
}

export interface NewsData {
  heading: string;
  subHeading: string;
  link: string;
}
